#include <bits/stdc++.h>
using namespace std;

using ll = long long;

int main()
{
    char s[1003], t[1003];
    gets(s), gets(t);
    printf("%d %d\n", strlen(s), strlen(t));
    printf("%s %s\n", s, t);
    return 0;
}